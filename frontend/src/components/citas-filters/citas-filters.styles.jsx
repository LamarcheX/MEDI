import styled from "styled-components";

export const FilterSection = styled.div`
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;

    p {
        margin: 0;
        align-self: center;
    }
`;
